# Realtime backend (server/) — used by `npm run server`
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Frontend — used by src/realtime/api.ts and RealtimeContext.tsx
VITE_SERVER_URL=http://localhost:4000
