services:
  postgres:
    image: postgres:17-alpine
    container_name: kundali-chat-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: kundali_chat
      POSTGRES_USER: kundali_chat
      POSTGRES_PASSWORD: kundali_chat_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kundali_chat"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: kundali-chat-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
